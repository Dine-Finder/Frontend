import { createTheme } from '@mui/material/styles';


const theme = createTheme({
  palette: {
    primary: {
        main: 'rgb(244, 131, 40)',
    },
    secondary: {
        main: 'rgb(4, 106, 180)',
    },
    tertiary:{
        main: 'rgb(188, 190, 196)',
    }
  },
});

export default theme;